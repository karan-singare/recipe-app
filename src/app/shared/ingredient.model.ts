export class Ingredient {
  /**
   * constructor(public name: string, public amount: number)
   * The aboev syntax create properties  of the class with the same name as args
   * and initialize them with the value passed to the constructor
   */
  constructor(public name: string, public amount: number) {}
}
